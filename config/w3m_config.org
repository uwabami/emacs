# -*- mode: org; coding: utf-8-unix; indent-tabs-mode: nil -*-
#
# Copyright(C) Youhei SASAKI All rights reserved.
# $Lastupdate: 2013/04/25 19:02:36$
# License: Expat
#
#+TITLE: emacs-w3mの設定
#+AUTHOR: Youhei SASAKI
#+EMAIL: uwabami@gfd-dennou.org
#+SETUPFILE: ./export-config.org
#+BEGIN_HTML
---
layout: emacs
title: emacs-w3m の設定
---
#+END_HTML
* 始めに
  ブラウザ自体としてはあんまり使っていないけれど,
  Wanderlust での HTML メールの表示なんかに必要で割と重宝している.
* 読み込み
  #+BEGIN_SRC emacs-lisp
    (require 'w3m-load)
  #+END_SRC
* 設定ファイルの置き場所
   #+BEGIN_SRC emacs-lisp
   (setq w3m-init-file
         (concat my:user-emacs-config-directory "w3m_config"))
   #+END_SRC
* cookie を使う
   #+BEGIN_SRC emacs-lisp
   (setq w3m-use-cookies t)
   #+END_SRC
* tab を使う
   #+BEGIN_SRC emacs-lisp
   (setq w3m-use-tab t)
   #+END_SRC
* 初期ページの設定
   #+BEGIN_SRC emacs-lisp
     (setq w3m-home-page "http://www.gfd-dennou.org/member/uwabami/")
   #+END_SRC
* tmp ファイルの置き場所
   =my:user-emacs-etc-directory= にしている.
   #+BEGIN_SRC emacs-lisp
     (setq w3m-default-save-directory my:user-emacs-temporary-directory)
     (setq w3m-arrived-file
           (concat my:user-emacs-temporary-directory "w3m-arraived"))
     (setq w3m-cookie-file
           (concat my:user-emacs-temporary-directory "w3m-cookie"))
     (setq w3m-form-textarea-directory
           (concat my:user-emacs-temporary-directory "w3m-textarea"))
     (setq w3m-bookmark-file
           (concat my:user-emacs-temporary-directory "w3m-bookmark"))
   #+END_SRC
* 右端の 10 文字前で折り返し
  #+BEGIN_SRC emacs-lisp
     (setq w3m-fill-column -10)
  #+END_SRC
* Footnotes
  - Emacs 上で w3m を動かそう: [[http://emacs-w3m.namazu.org/index-ja.html]]
